# Build Project

Build the project for production.

Run: `npm run build`

This will:
1. Type-check the TypeScript code
2. Bundle with Vite
3. Output to `dist/` folder

Report any build errors and help fix them if they occur.
