# Lint Code

Run ESLint on the codebase to check for code quality issues.

Run: `npm run lint`

If errors are found:
1. List all errors grouped by file
2. Offer to fix auto-fixable issues with `npm run lint -- --fix`
3. For manual fixes, explain what needs to be changed
